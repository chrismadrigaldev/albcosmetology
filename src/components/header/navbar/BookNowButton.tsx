export const BookNowButton = () => {
    return (
        <button style={{
            alignItems: "center",
			border: "none",
            background: "none",
            cursor: "pointer",
			padding: "0.3rem",
        }}>
            Book Now
        </button>
    );
};